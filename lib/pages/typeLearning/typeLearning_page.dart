import 'package:flutter/material.dart';

class TypeLearningPage extends StatelessWidget {
  const TypeLearningPage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(
          child: Text('Type Learning'),
        ),
      ),
    );
  }
}
