import 'package:flutter/material.dart';

class SavedActivitiesScreen extends StatelessWidget {
  const SavedActivitiesScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: const Center(
        child: Text('SavedActivities Screen'),
      ),
    );
  }
}
